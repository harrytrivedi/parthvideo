# Use the official PHP image with Apache
FROM php:8.2-apache

# Copy your project files to the web root
COPY . /var/www/html/

# (Optional) Expose port 80 – Render automatically maps this
EXPOSE 80
